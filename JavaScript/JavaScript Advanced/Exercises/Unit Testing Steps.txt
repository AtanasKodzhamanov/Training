npm init -y - creates package.json
npm install chai, mocha

Set-ExecutionPolicy -ExecutionPolicy Bypass -Scope CurrentUser (Must have this setting set in the terminal)
Must have only one file in the folder, with the folder's name not having any spaces

have the test in test.js

to run type:
mocha


module.exports=companyAdministration (export a module in the code you are testing)

let {assert, expect} = require("chai")
let companyAdministration=require("./companyAdministration")

describe("nameoffiletobetested",()=>{
    describe("funcname", ()=>{
	it("test 1",()=>{
            expect(()=>nameoffiletobetested.funcname(inputs you are testing)).to.throw(if an expected error message pops up) //remove ()=> for other comparators
        })
    })
    describe("funcname", ()=>{
        
    })
    describe("funcname", ()=>{
        
    })
})

.to.equal()
.to.throw()
.to.deep.equal() - when comparing arrays



