npm init -y - creates package.json
npm install chai, mocha

let expect = requre("chai")
let companyAdministration=require("./companyAdministration")

describe("nameoffiletobetested",()=>{
    describe("funcname", ()=>{
	it("test 1",()=>{
            expect(()=>nameoffiletobetested.funcname(inputs you are testing)).to.throw(if an expected error message pops up) //remove ()=> for other comparators
        })
    })
    describe("funcname", ()=>{
        
    })
    describe("funcname", ()=>{
        
    })
})

.to.equal()
.to.throw()
.to.deep.equal() - when comparing arrays

Go to package.json 
change to "test": "mocha"

npm run test